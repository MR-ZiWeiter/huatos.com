import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { DocDownloadComponent } from './doc-download.component';

describe('DocDownloadComponent', () => {
  let component: DocDownloadComponent;
  let fixture: ComponentFixture<DocDownloadComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ DocDownloadComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(DocDownloadComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
