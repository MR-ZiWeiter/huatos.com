import { Params } from '@angular/router';
export interface IBreadcrumb {
  label: string; // 用于显示的path
  params?: Params; // 跳转时所带参数
  url: string; // 跳转的url
}
export interface Breadcrumb {
  Type_id: number;
  Type_Name: string;
  subnideNode?: Breadcrumb[];
}
