import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { OperatingSolutionComponent } from './operating-solution.component';

describe('OperatingSolutionComponent', () => {
  let component: OperatingSolutionComponent;
  let fixture: ComponentFixture<OperatingSolutionComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ OperatingSolutionComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(OperatingSolutionComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
